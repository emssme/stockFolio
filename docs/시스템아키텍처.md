# StockFolio 시스템 아키텍처

**문서 버전:** 1.0  
**작성일:** 2026-04-16  
**프로젝트명:** StockFolio — 실시간 주식/코인 포트폴리오 트래커

---

## 목차

1. [전체 구성도](#1-전체-구성도)
2. [레이어 구조](#2-레이어-구조)
3. [인프라 구성](#3-인프라-구성)
4. [주요 흐름](#4-주요-흐름)
5. [기술 스택 선택 이유](#5-기술-스택-선택-이유)

---

## 1. 전체 구성도

```mermaid
graph TB
    subgraph Client["클라이언트 (Browser)"]
        React["React SPA"]
    end

    subgraph GCP["GCP (Google Cloud Platform)"]
        LB["Load Balancer / HTTPS"]

        subgraph AppServer["애플리케이션 서버"]
            SpringBoot["Spring Boot"]
            SpringSecurity["Spring Security (JWT Filter)"]
            WebSocketServer["WebSocket Handler"]
        end

        subgraph DataLayer["데이터 레이어"]
            MySQL["MySQL 8.0\n(영구 저장소)"]
            Redis["Redis\n(캐시 / 세션)"]
        end

        Scheduler["시세 수집 스케줄러"]
    end

    subgraph ExternalAPI["외부 시세 API"]
        KIS["한국투자증권 Open API\n(국내 주식)"]
        Finnhub["Finnhub\n(해외 주식)"]
        Binance["Binance WebSocket\n(암호화폐)"]
    end

    React -- "HTTPS REST" --> LB
    React -- "WSS WebSocket" --> LB
    LB --> SpringBoot
    LB --> WebSocketServer
    SpringBoot --> SpringSecurity
    SpringBoot --> MySQL
    SpringBoot --> Redis
    WebSocketServer --> Redis

    Scheduler -- "주기적 시세 수집" --> KIS
    Scheduler -- "주기적 시세 수집" --> Finnhub
    Binance -- "실시간 스트리밍" --> Scheduler
    Scheduler -- "quote:{ticker} TTL 10s" --> Redis
```

---

## 2. 레이어 구조

### 2.1 백엔드 패키지 구조

```
com.stockfolio
├── auth                    # 인증/인가
│   ├── controller          # /api/auth/**
│   ├── service
│   ├── dto
│   └── filter              # JWT 필터 (Spring Security)
├── user                    # 사용자 관리
│   ├── controller          # /api/users/**
│   ├── service
│   ├── domain              # User 엔티티
│   └── repository
├── asset                   # 자산 관리
│   ├── controller          # /api/assets/**
│   ├── service
│   ├── domain              # Asset, TradeHistory 엔티티
│   └── repository
├── portfolio               # 포트폴리오 집계
│   ├── controller          # /api/portfolio/**
│   ├── service
│   ├── domain              # PortfolioSnapshot 엔티티
│   └── repository
├── chart                   # 차트 데이터
│   ├── controller          # /api/chart/**
│   └── service
├── quote                   # 시세 관련
│   ├── scheduler           # 외부 API 시세 수집
│   ├── service             # Redis 캐시 처리
│   └── websocket           # WebSocket Handler
└── global
    ├── config              # Security, Redis, WebSocket 설정
    ├── exception           # 전역 예외 처리
    └── response            # 공통 응답 구조
```

### 2.2 계층 역할

| 계층 | 역할 |
|------|------|
| Controller | HTTP 요청/응답 처리, DTO 변환 |
| Service | 비즈니스 로직, 트랜잭션 관리 |
| Repository | DB 접근 (Spring Data JPA) |
| Domain | 엔티티 정의 |
| Filter | JWT 토큰 검증 (요청마다 실행) |
| Scheduler | 외부 API 시세 주기적 수집 → Redis 저장 |

### 2.3 프론트엔드 구조

```
src
├── pages
│   ├── LoginPage
│   ├── SignupPage
│   ├── DashboardPage       # 총 자산 현황, 파이 차트
│   ├── PortfolioPage       # 자산 목록, 실시간 수익률
│   ├── ChartPage           # 수익률 추이 차트
│   └── MyPage              # 프로필 관리
├── components              # 재사용 UI 컴포넌트
├── hooks
│   ├── useWebSocket        # 시세 WebSocket 연결/재연결
│   └── useAuth             # JWT 토큰 관리
├── api                     # REST API 호출 (axios)
└── store                   # 전역 상태 (React Context or Zustand)
```

---

## 3. 인프라 구성

### 3.1 GCP 배포 구성

```
인터넷
  │
  ▼
Cloud Load Balancing (HTTPS/WSS 종단)
  │
  ▼
Cloud Run / Compute Engine
  ├── Spring Boot 서버 (포트 8080)
  └── Nginx (리버스 프록시)
  │
  ├── Cloud SQL (MySQL 8.0)
  └── Memorystore (Redis)
```

| 구성 요소 | GCP 서비스 | 역할 |
|-----------|-----------|------|
| 애플리케이션 | Cloud Run 또는 Compute Engine | Spring Boot 실행 |
| 데이터베이스 | Cloud SQL (MySQL 8.0) | 영구 데이터 저장 |
| 캐시 | Memorystore (Redis) | 시세 캐시, Refresh Token |
| 로드밸런서 | Cloud Load Balancing | HTTPS/WSS 처리, 트래픽 분산 |
| 스토리지 | Cloud Storage | 정적 파일 (React 빌드 결과물) |

### 3.2 Redis 키 설계

| Key 패턴 | TTL | 용도 |
|----------|-----|------|
| `quote:{ticker}` | 10s | 종목 시세 캐시 |
| `refresh:{userId}` | 7d | Refresh Token 저장 |

---

## 4. 주요 흐름

### 4.1 JWT 인증 흐름

```mermaid
sequenceDiagram
    participant C as 클라이언트
    participant F as JWT Filter
    participant S as Spring Security
    participant DB as MySQL

    C->>S: POST /api/auth/login
    S->>DB: 이메일/비밀번호 검증
    DB-->>S: 사용자 정보
    S-->>C: Access Token (30분) + Refresh Token (7일)

    Note over C,F: 이후 API 요청

    C->>F: GET /api/assets (Authorization: Bearer {token})
    F->>F: JWT 서명 검증 + 만료 확인
    F-->>C: 유효 → 요청 통과 / 만료 → 401 반환

    Note over C,S: Access Token 만료 시

    C->>S: POST /api/auth/reissue (refreshToken)
    S->>DB: Refresh Token 검증
    S-->>C: 새 Access Token + 새 Refresh Token (로테이션)
```

### 4.2 실시간 시세 흐름

```mermaid
sequenceDiagram
    participant Binance as Binance WS
    participant KIS as 한국투자증권 API
    participant Finnhub as Finnhub API
    participant Sched as 시세 스케줄러
    participant Redis as Redis
    participant WS as WebSocket Handler
    participant C as 클라이언트

    loop 10초마다
        Sched->>KIS: 국내 주식 시세 요청
        Sched->>Finnhub: 해외 주식 시세 요청
        KIS-->>Sched: 현재가
        Finnhub-->>Sched: 현재가
        Sched->>Redis: quote:{ticker} 저장 (TTL 10s)
    end

    Binance-->>Sched: 코인 시세 실시간 Push
    Sched->>Redis: quote:BTC 등 저장 (TTL 10s)

    C->>WS: WebSocket 연결 + SUBSCRIBE ["005930", "BTC"]
    loop 시세 변경 감지 시
        WS->>Redis: quote:{ticker} 조회
        Redis-->>WS: 현재가
        WS-->>C: QUOTE_UPDATE 메시지
    end
```

### 4.3 자산 등록 및 수익률 계산 흐름

```mermaid
sequenceDiagram
    participant C as 클라이언트
    participant AS as AssetService
    participant DB as MySQL
    participant Redis as Redis

    C->>AS: POST /api/assets (종목, 수량, 매수가)
    AS->>DB: assets 저장 / 기존 종목이면 avg_purchase_price 재계산
    AS->>DB: trade_history BUY 이력 저장
    AS-->>C: 등록된 자산 정보 반환

    C->>AS: GET /api/assets (목록 조회)
    AS->>DB: 보유 자산 조회
    AS->>Redis: quote:{ticker} 현재가 조회
    AS->>AS: 평가금액 / 수익금액 / 수익률 계산
    AS-->>C: 수익률 포함 자산 목록 반환
```
